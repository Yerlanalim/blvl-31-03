import { redirect } from 'next/navigation';

/**
 * Корневая страница приложения.
 * Автоматически перенаправляет пользователя на страницу карты уровней (/map).
 * Это обеспечивает правильную навигацию в приложении, направляя пользователей 
 * на главную функциональную страницу.
 */
export default function HomePage() {
  // Выполняем редирект на страницу карты уровней
  redirect('/map');
}
